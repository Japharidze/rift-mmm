# Copy to .env and fill in. Committed as documentation of required keys.
POSTGRES_USER=rift
POSTGRES_PASSWORD=change-me
POSTGRES_DB=rift_mmm

# Where the app reaches the database. Correct while the app runs on the host;
# becomes the compose service name ("db") if the app is ever containerised.
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
